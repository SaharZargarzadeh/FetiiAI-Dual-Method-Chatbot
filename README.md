# 🚌 FetiiAI — Dual-Method Chatbot (Rule-based + RAG)

Two methodologies:
1) Rule-based (offline) with optional GPT intent assist.
2) RAG (SentenceTransformers embeddings + SQLite) with optional GPT synthesis.

Run:
    pip install -r requirements.txt
    streamlit run app.py
